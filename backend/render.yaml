services:
  - type: web
    name: sparktales-backend
    env:  python
    buildCommand: pip install --upgrade pip && pip install --only-binary :all: -r backend/requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
